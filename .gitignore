# macOS
.DS_Store

# ContractIQ now lives in its own repo (github.com/mikinmacwan-pm/contractiq),
# nested here as an independently git-tracked directory with its own remote.
# Ignored from dev-os's perspective so it's never double-tracked or
# accidentally swept up by a broad `git add`.
contractiq/

# editor/OS noise
*.swp
.vscode/
.idea/
